@using VendorOrder.Models;

@{
  Layout = "_Layout";
}

@if (Model.Count == 0)
{
  <h3>No Active Orders have been placed</h3>
}

@foreach (Order order in Model)
{
  <h3><a href="vendor/@order.VendorId/order/@order.Id"></a></h3>
}